# Actuarial Platform — Insurance Insights Board

보험 정보 공개 게시판 (Next.js 15 App Router · TweakCN/shadcn · Supabase · Anthropic Claude API). 설계서: `insurance-insights-board-design-v1.0.md`(v1.1).

## 하네스: Insurance Insights Board

**목표:** 설계서 v1.1 기반으로 게시판(DB/API/UI/요약)을 에이전트 팀으로 빌드.

**트리거:** 게시판 빌드/DB·RLS/API/UI·페이지/요약 구현, 또는 결과 수정·부분 재실행·업데이트 요청 시 `insurance-board-builder` 스킬을 사용하라. 보험이론 사전(/theory) 자료 게시·커버 생성·재생성은 `theory-publisher` 스킬을 사용하라. 데이터 예제/분석(/datalab) 자료 게시·수정·워크북 버전 추가는 `datalab-publisher` 스킬을 사용하라. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-09 | 초기 구성 (에이전트 5 + 스킬 3 + 오케스트레이터 1) | 전체 | 설계서 §5 하네스 적용, 서브에이전트→에이전트 팀 전환 |
| 2026-06-09 | 앱 빌드(기반/API/UI/요약) + DB/RLS 산출 | app/, lib/, components/, output/ | 설계서 v1.1 구현, build·typecheck 통과 |
| 2026-06-09 | 기존 뉴스 프로젝트 공존 + 브리지 뷰 초안 | .env, output/integration_bridge_view.draft.sql, docs/domain | 동일 Supabase에 additive(ib_ 격리), 향후 합산은 분리+브리지 뷰(B) |
| 2026-06-09 | "보험 뉴스" 섹션(/news) 추가 | app/(public)/news, lib/news.ts, components/feature/News* , SiteNav | ins_news_articles(읽기 공개) 직접 연동. 뉴스 앱 배포 유지, 보드는 읽기. build 통과(13 라우트) |
| 2026-06-11 | tkLeen 아이덴트 애니메이션 (HeroIdent) | components/feature/HeroIdent·HeroSection·SiteNav, globals.css, tailwind | 히어로 우측(PC)+모바일 헤더 마크. 픽셀 맵·타이밍은 tkleen-hero-animation.html 원본 유지 |
| 2026-06-11 | 카드 입체화 + 카드 타이틀 구별 + 홈 개선 | globals.css, tailwind, layout, PostCard·HeroSection·SummaryPanel·PdfViewer·ui/card, 홈, tweakcn-tesla-theme 스킬 | 사용자 결정: 카드 엘리베이션(2단 섀도+lift) 허용, 타이틀 Noto Serif KR 600·navy, 페이지 캔버스 cream·격자점 히어로·섹션 픽셀 액센트 |
| 2026-06-11 | "보험이론 사전" 섹션(/theory) — 폴더 기반 정적 자료실 | app/(public)/theory, lib/theory.ts, public/theory/{life,general}, SiteNav | public/theory/<주제>/에 .html·.pdf 추가→빌드 시 자동 목록화(주제: 생명/손해, 추가는 THEORY_TOPICS 배열). HTML 임베드 열람+PDF 병행, 같은 파일명은 한 항목 |
| 2026-06-11 | 카드 타이틀 고딕 600·로고 블루(--brand-sky)로 변경 | PostCard, 홈, theory 페이지, layout, tailwind, globals, 테마 스킬 | 사용자 결정: 세리프(Noto Serif KR)·네이비 안 철회, 세리프 폰트 로딩 제거 |
| 2026-06-11 | 풀링 아이덴트(PoolIdent) + 아이덴트 시안 폴더 design/idents/ | components/feature/PoolIdent.tsx, app/(public)/theory/[topic], design/idents/ | 이론 사전 헤더 우측(PC) 무질서→질서 루프. 모션 시안 HTML은 design/idents/에서 관리(README에 변환 규칙·매핑) |
| 2026-06-11 | 이론 사전 커버 일러스트(플랫 카툰 SVG) + 뷰어 PDF 하단 다운로드 + theory-publisher 스킬 | lib/theory.ts, theory 목록/뷰어, public/theory/life/*.svg, .claude/skills/theory-publisher | 커버 스타일 A(플랫 카툰) 사용자 확정. 자료명.svg 자동 매칭, 게시+커버 생성은 theory-publisher 스킬로 자동화 |
| 2026-06-11 | 이론 사전 카드 단순화(제목↑커버↓) + PDF 노출 제거 | theory 목록/뷰어, lib/theory, README×2, theory-publisher 스킬 | 사용자 결정: 제목·그림 클릭=본문 진입 단일 동선, 보조 링크 없음, pdf는 폴더 보관만(html 필수). 스킬에 비협상으로 반영 |
| 2026-06-11 | "만든이"(/about) 추가 — AI4Insurance 소개 이식 | app/(public)/about, SiteNav | AI4Insurance 프로젝트의 about(소개·신뢰지표·경력·성과·논문 8편·전문분야·철학)만 이식, 메뉴는 관리자 왼쪽. 나머지 섹션은 미이식 |
| 2026-06-11 | 이론 사전 표시 제목 정리 규칙 | lib/theory.ts(displayTitle), README×2, theory-publisher 스킬 | 사용자 결정: '해설서' 단어 제거, `_`→띄어쓰기 (URL·파일명은 유지) |
| 2026-06-11 | 이론 사전 제목 영문 병기 + '보험통계' 주제 추가 | lib/theory.ts(titleFromHtml, THEORY_TOPICS), public/theory/statistics, README×3, theory-publisher 스킬 | 제목은 html <title> "한글명 (영문명)" 자동 추출(폴백: 파일명 규칙). 주제 탭 3개: 생명/손해/보험통계 |
| 2026-06-11 | "모델분석/업무지원앱"(/apps) 추가 — App Collecter 카드 7종 이식 | app/(public)/apps, SiteNav | 모델분석 2종(보험료 자동산출·머신러닝 자동 플로우) + 업무지원 5종(강의 지원·PDF Master·Actuary Pro Calc·보험수리 화이트보드·이미지 수식변환기). 메뉴 8개 → 데스크톱 풀 메뉴 xl부터(미만 햄버거) |
| 2026-06-11 | 모델선택 아이덴트(ModelSelectIdent) — /apps 헤더 하단 | components/feature/ModelSelectIdent.tsx, globals.css, app/(public)/apps, design/idents | tkleen-model-selection-animation.html 이식(노드 등장→펄스 3웨이브→AUC 카운트업→GBM 승자 강조→분포 출력→최적 경로 루프). PC(lg+) 전용 |
| 2026-06-11 | 이론 사전 '재보험' 주제 추가 + 제목 추출 버그 수정 | lib/theory.ts(THEORY_TOPICS, titleFromHtml), public/theory/reinsurance, theory-publisher 스킬 | 주제 탭 4개: 생명/손해/재보험/통계. titleFromHtml은 "— 학습 해설서" 꼬리만 제거(제목 내 대시 보존) |
| 2026-06-11 | 전 자료(41종) '한국보험시장 현황' 섹션 추가 + 스킬 필수 규칙화 | public/theory/**/*.html, theory-publisher 스킬 | 30년차 계리사 페르소나, 이론↔한국 시장 일치/차이 중심. 상품=국내 판매상품, 계리·통계=IFRS17·K-ICS, 재보험=국내 방식·공동재보험. 웹서치 앵커(5세대 실손 2026.5, K-ICS 130%/기본자본 2027, 10회 생명표 등) 스킬에 수록 |
| 2026-06-13 | 디자인 v2(입체화) + 해외 자료 iframe→네이티브 전환 + 일본 FSA 심사사례 지식베이스화 | globals.css·tailwind, app/(public)/global/**, components/feature/{fsa,global}/**, data/japan-fsa·japan-life·japan-life-trends, lib/{japanFsa,global}, 홈·about·apps·theory, 테마 스킬 | 사용자 디자인 리뷰 반영: 벤토 그리드·스탯 카운트업·글래스 필터바·워터마크·칩 8색·헤딩 600/700(디자인 비협상 v2 완화). FSA 135건(15호) cases.html→cases.json 무손실 추출 후 테마 13종·용어집 91·한국KB 26항목·사례별 인리치먼트(tldr/keyPhrase/한국 맥락) 부가 레이어로 파셋 탐색기 구현. 동향2025(42도표·22표)·변천 가이드 네이티브화. 레거시 /global/[slug]는 redirect 보존 |
| 2026-06-14 | /posts 헤더를 카테고리명+서브타이틀로, 카테고리별 "항목(subsection)" 그룹핑 + 국내 '상품 정보' 정적 자료 12종 추가 | app/(public)/posts/page.tsx, app/(public)/domestic/products/[name], lib/{postSections,domesticProducts}, components/feature/ResourceCard, public/domestic/products/*.html | 사용자 결정: H1 '자료실'→선택 메뉴 이름+설명(서브타이틀). 배타권→질병관련/기타(비용등), 국내→보험사 매각정보(매각 2)/상품 정보(실손+신규 12). 항목 분류는 ib_posts 스키마 미변경, lib/postSections.ts 제목 키워드 코드매핑. 신규 12 HTML은 보험이론 패턴(iframe 임베드)로 원본 무수정 표시 |
| 2026-06-14 | /posts 필터: 카테고리 선택 시 카테고리 탭→항목 탭 전환 + 실손 카드 파일 대체 | components/feature/PostFilters.tsx, app/(public)/posts/page.tsx, lib/domesticProducts.ts, public/domestic/products/silson-insurance-generations.html | 사용자 요청: 카테고리는 상단 내비로 이동하므로 카테고리 선택 후엔 중복 카테고리 탭 대신 해당 항목(서브) 탭 노출(`sub` 쿼리 필터). 기존 실손 DB글(id 4b1a2284…)은 is_published=false 처리하고 정적 파일로 대체 |
| 2026-06-14 | /apps 모델분석 섹션 지그재그 배치 + Life Matrix 파이프라인 아이덴트 임베드 | app/(public)/apps/page.tsx, public/idents/tkleen-lifematrix-pipeline-theme.html | 사용자 요청: 보험료카드↔파이프라인 / 파이프라인↔ML카드 지그재그(2×2, md+). 애니메이션 위주(iframe h440)+하단 간단 설명 캡션. 같은 파일 2회 사용(사용자 명시). 임베드 파일의 progress rect width 음수 버그만 Math.max(0,…)로 최소 수정(콘솔 189에러→0) |
| 2026-06-14 | /apps 헤더 아이덴트 교체: ModelSelectIdent → 모듈러 빌드 애니메이션 | app/(public)/apps/page.tsx, public/idents/tkleen-modular-build-animation.html | 사용자 요청: 제목 바로 아래 아이덴트를 tkleen-modular-build-animation.html로 변경. iframe 임베드(흰 카드로 감싸 크림 캔버스에 자연스럽게), PC(lg+) 전용 유지. ModelSelectIdent import/사용 제거(컴포넌트 파일은 보존). 콘솔 에러 0 |
| 2026-06-14 | /apps ML카드 좌측 애니메이션 교체 + 아이덴트 배경 정리 | app/(public)/apps/page.tsx, public/idents/tkleen-ml-training-pipeline.html | 머신러닝 자동분석 카드 왼쪽을 tkleen-ml-training-pipeline.html(데이터 적재→평가)로 교체. PipelineIdentCell에 src/title/heightClass prop화(와이드형은 h-340). ml-training progress rect width 음수 버그 Math.max(0,…) 클램프 |
| 2026-06-14 | /theory·/apps 카드+아이덴트 파란 파스텔 배경 | lib/utils.ts(bluePastelFor), app/(public)/theory/[topic]·apps/page.tsx, public/idents/*.html(3종) | 사용자 요청: 카드마다 파란 계통 파스텔(6음영) 자동배정. 아이덴트는 body background:transparent로 바꿔 감싼 카드의 파란 배경이 비치게(흰 모듈 타일이 대비로 돋보임). 이론 카드·앱 카드·공공DB 카드·헤더/파이프라인 아이덴트 일괄 적용 |
| 2026-06-14 | /apps 아이덴트 3종 페이지 배경(크림) 일치 + 상단 아이덴트 텍스트 제거 | app/(public)/apps/page.tsx, public/idents/tkleen-modular-build-animation.html | 사용자 요청: 아이덴트 카드 테두리·그림자·파란배경 제거→투명(페이지 cream 비침), 그래픽만 페이지에 녹아듦(콘텐츠 카드는 파란 파스텔 유지). 상단 모듈빌드 아이덴트는 .head/.caption/.concept-note/버튼 display:none으로 위·아래 텍스트 제거(그래픽만). 헤더 높이 420 |
| 2026-06-29 | 홈 전용 대형 tkLeen 브랜드 배경 워터마크(BrandBackdrop) + 홈 카드 반투명(글래스)화 | lib/tkleenMark.ts, components/feature/BrandBackdrop.tsx, app/globals.css, app/(public)/page.tsx | 사용자 요청: 메인에서만 카드를 반투명 프로스티드로 바꿔 뒤의 대형 브랜드 마크가 비치게. 마크는 흩어짐↔결집 무한 루프 + 38s 드리프트(fixed -z-10, pointer-events:none). 픽셀맵은 HeroIdent와 공유(lib/tkleenMark). 투명도: 스탯 스트립·최신 자료 34%, 콘텐츠 한눈에 벤토 22%. 다른 페이지는 .home-glass 스코프로 영향 없음(불투명 유지). 위치/크기: width min(510px,49vw)·중심 고정(padding-top=388−높이/2 보정). 작은 HeroIdent는 유지. 디자인 비협상 '워터마크 ≤5%' 규칙은 본 배경에 한해 사용자 승인으로 완화 |
| 2026-07-10 | 홈 워터마크를 회전 지구본(GlobeBackdrop)으로 교체 + tkLeen 마크(BrandBackdrop)는 배타적 사용권 목록으로 이전 | components/feature/GlobeBackdrop.tsx, public/data/land-110m.json, app/globals.css, app/(public)/page.tsx, app/(public)/posts/page.tsx, BrandBackdrop 주석 | 사용자 요청: '지구본 워터마크 데모.html'(standalone JS) 이식 — 보험료 규모 Top10 국가·흐름 아크·대륙 점묘(world-atlas land-110m 로컬 번들). 색상은 토큰 매핑(#5C67F2→--primary, 오렌지→--chip-amber-fg), 구체 그라데이션은 비협상 준수로 플랫 화이트, 폰트 800→700. 뷰포트 중앙 고정 72vmin·불투명도 0.15(데모 기본). /posts?category=exclusive-rights에서만 BrandBackdrop+.home-glass(카드 34%/22% 기존 컨벤션) 적용, 다른 카테고리는 불투명 유지. reduced-motion은 정적 1프레임 |
| 2026-07-10 | 지구본 워터마크 가시성 강화 + 홈 글래스 카드 투명도 상향 (2차) + 중앙 문구 앰버 | app/globals.css, app/(public)/page.tsx, GlobeBackdrop | 사용자 요청 2회: 워터마크 0.15→0.28→0.42, --card-glass-bg 0.34→0.2→0.1, 벤토 0.22→0.12→0.06, 관련링크 0.35→0.2→0.1, hover 0.52→0.4→0.28, blur 7→4→2px. 중앙 '보험료 규모' 문구는 블루와 구분되는 앰버(--chip-amber-fg, α0.85)로 강조. .home-glass 공유라 배타권 목록 카드도 동일 |
| 2026-07-10 | 지구본 86vmin 확대 + 국가명·달러 라벨 + 중앙 문구 '보험료 규모(2024)' | GlobeBackdrop, app/globals.css | 사용자 요청: 노드 옆에 국가명(잉크)+보험료 달러 금액(앰버, $3.65T/$810B 포맷) 라벨을 흰 halo로 표시(구체 좌/우측에 따라 안쪽 배치). 가독성을 위해 CSS 전체 opacity(--globe-backdrop-opacity 폐기) 대신 캔버스 레이어 알파로 분리: 배경(구체·경위선·대륙·아크) BG_ALPHA 0.4, 전경(노드·라벨·중앙 문구) FG_ALPHA 0.85. 구체는 캔버스의 86%로 그려 가장자리 라벨 여백 확보 |
| 2026-07-10 | 지구본 대륙을 점묘→실제 지도(대륙면+해안선)로 + 중앙 문구 옅게 | GlobeBackdrop, package.json(d3-geo·topojson-client) | 사용자 요청: 진짜 지구본 지도 형태 — d3-geo 정사영(geoOrthographic, clipAngle 90)으로 land-110m을 채운 대륙면(rgba primary 0.2)+해안선(0.5)으로 렌더. 커스텀 proj와 d3 rotate([lam,-TILT]) 좌표 정합 수식 검증(노드·아크가 지도 위 정위치). 경위선도 d3 graticule로 대체, 점묘 코드(pointInLand·topoToLandDots) 제거. 중앙 문구는 CENTER_ALPHA 0.4로 분리(옅게, halo 제거). 이어서 국가명(R×0.048)·순위 숫자(rad×1.15, 최대 18px)만 확대 — 달러 금액은 R×0.036 유지 |
| 2026-07-10 | 지구본 라벨 겹침 회피 + 북반구 중심 시점 + 위치 하향 + 100vmin 확대 | GlobeBackdrop(TILT 20→35, 라벨 배치 알고리즘), globals.css(.globe-backdrop translateY 5vh·100vmin) | 사용자 요청: ① 라벨은 순위순 그리디 배치 — 노드 영역 포함 배치목록과 겹치면 옆(위/아래 밀기)→노드 위/아래→반대편 순 8개 후보에서 첫 빈자리 선택. ② TILT 35°로 북반구(Top10 전부 북반구) 중심 회전. ③ 지구본을 화면 중앙에서 5vh→10vh 아래로. ④ 크기 86→100vmin. ⑤ 글자 확대 2차: 국가명 R×0.056·금액 R×0.044·순위 rad×1.3(최대 21px) |
| 2026-07-12 | 만든이(/about) 초상 캐리커처 모자이크 워터마크(PortraitBackdrop) | components/feature/PortraitBackdrop.tsx, app/(public)/about, app/globals.css, public/about/portrait/*.jpg(6장) | 사용자 요청: '초상_워터마크_모자이크.html' 이식 — 캐리커처 6장(원본→연필→수채화→선셋→셀→팝아트)을 hold 2.6s→모자이크 타일 시차 전환 2s로 순환. 내장 base64 이미지는 public/about/portrait/로 추출(~610KB). 크기는 데모 min(70vmin,520px)→min(50vmin,380px) 축소(너무 크지 않게), opacity 0.16, fixed 뷰포트 중앙·-z-10·클릭 통과(워터마크 3종 컨벤션). reduced-motion은 정적 1프레임(원본), 카드 글래스화는 미적용(불투명 유지) |
| 2026-07-12 | 초상 워터마크 프레임 얼굴 정렬(머리 끝 통일) + 만든이 카드 반투명화 + 워터마크 불투명도 0.16→0.12 | PortraitBackdrop(FRAMES 앵커·alignRect·TOP_T/CHIN_T), app/(public)/about, globals.css(.about-glass, .portrait-backdrop opacity) | 사용자 요청 2건: ① 프레임별 프레이밍 상이(머리 끝 y 1.5%~17.6%)로 전환 시 얼굴이 튐 — 프레임별 앵커(top 머리끝/chin 턱/cx 얼굴중심, 픽셀 명도 스캔+시각 검증으로 측정)를 상수로 두고 모든 프레임에서 머리 끝→캔버스 2%·턱→77%에 오도록 소스 사각형 정규화(cover 폐기, 위치+스케일 동시). 이미지 교체 시 앵커 재측정 필요. ② 만든이 카드 불투명 → .about-glass .glass-card(백색 40%+blur 2px, 홈 글래스 0.1보다 온건) — 스탯 4·성과·연구 목록·전문분야 칩·철학 카드 적용 |
| 2026-07-12 | 상단 메뉴 풀 노출 브레이크포인트 xl→lg | components/feature/SiteNav.tsx | 사용자 보고: PC에서도 햄버거만 노출(뷰포트 1280px 미만 — Windows 배율·창 크기). lg(1024px)부터 풀 메뉴 — lg~xl 구간은 압축(text 13px·gap-2.5), xl부터 기존(15px·gap-4). 햄버거·드롭다운은 lg 미만. 1024px 한 줄 수용·1019px 햄버거 전환 Playwright 실측 검증 |
| 2026-07-13 | "데이터 예제/분석"(/datalab) 섹션 신설 — 엑셀 데이터 분석 게시판 + 웹 엑셀 열람/편집·자동저장 + datalab-publisher 스킬 | app/(public)/datalab/**, components/feature/datalab/**, lib/datalab{,-xlsx,-files}.ts, app/api/datalab/**, types, SiteNav, 홈 벤토, output/datalab_{schema,rls_tests,seed}.sql·datalab_api_contract.json, scripts/datalab-{inspect,publish}.mjs, .claude/skills/datalab-publisher | 사용자 요청: 만든이 왼쪽 새 카테고리. DB는 ib_data_posts/ib_data_files 신규(additive)+기존 ib-attachments 버킷 재사용(datalab/{post_id}/ 경로), 게시=DB+Storage라 재배포 불필요. 카드에 출처·모델·도구 즉시 노출, 상세는 content jsonb 구조화(개요/특성/레이아웃/분석방법/이미지/링크). 웹 엑셀 = fortune-sheet+luckyexcel(클라)+exceljs(서버 변환) — 열람 공개(allowEdit false)·편집/자동저장(4s 디바운스)은 관리자 전용, 저장마다 v(n+1) 새 버전(원본 v1 영구 보존 — VBA·차트·Python in Excel은 웹 저장본에서 소실되므로 원본만 유지). 내비 9개: lg 12px·gap-2→xl 13px→2xl 15px 압축(1024px 한 줄 Playwright 실측). DB는 2026-07-13 Management API로 라이브 적용 완료(schema+seed, 계정 taekyoleen@gmail.com/프로젝트명 insurance-article) + 데모 게시물 2건(seed 1·워크북 데모 1) |
| 2026-07-13 | /datalab "Excel로 직접 작업하기"(내 OneDrive Web-Excel 열기 + MS Excel 다운로드) + 상세 본문 확장(주요 엑셀 함수·파이썬 코드 분석·산출 결과) | lib/msgraph.ts, components/feature/datalab/ExcelLaunchPanel.tsx, app/(public)/datalab/[slug]{,/edit}/page.tsx, types/index.ts, scripts/datalab-inspect.mjs, .claude/skills/datalab-publisher, output/msgraph_setup.md, .env.example | 사용자 결정: C안(Graph 사본 복사) — MSAL PKCE(Files.ReadWrite)로 방문자 본인 OneDrive `DataLab/`에 사본 업로드(rename, 4MB 초과 업로드 세션) 후 Excel 웹 webUrl 새 탭. 이용 조건 상세 고지(무료 MS계정 가능·VBA 웹 실행 불가·Python은 M365·편집은 사본에만). 다시 게시는 기존 절차 별도 유지, fortune-sheet 뷰어/편집기 유지. `NEXT_PUBLIC_MS_GRAPH_CLIENT_ID` 미설정 시 비활성 폴백(Azure 앱 등록: output/msgraph_setup.md). content에 keyFunctions(365 신규·통계 함수 필수 해설)/pythonAnalysis/results 신설, inspect가 함수별 빈도·샘플과 =PY( 코드 추출(함수 분석은 원본 v1로), 스킬 작성 규칙 갱신 |
| 2026-07-13 | Web-Excel 로그인 안정화 — interaction_in_progress 자가 복구 + 전용 리디렉션 페이지 | lib/msgraph.ts, public/msal-redirect.html, output/msgraph_setup.md | 사용자 보고 2건: ① 팝업 강제 닫힘 후 잔류 플래그(msal interaction.status)로 재시도 전부 실패 → 플래그 제거+1회 자동 재시도. ② 팝업이 홈 전체를 로드한 채 안 닫힘 → MSAL 권장 blank page(/msal-redirect.html)로 redirectUri 변경. **Entra SPA 리디렉션 URI를 경로 포함으로 재등록 필요**(루트만 등록 시 AADSTS50011) |
| 2026-07-13 | Web-Excel timed_out 근본 수정 — msal v5 redirect-bridge 실행 리디렉션 페이지 + 팝업 대기 5분 | app/msal-redirect/page.tsx, next.config.mjs(rewrite), lib/msgraph.ts, output/msgraph_setup.md, public/msal-redirect.html 삭제 | msal-browser v5는 본창이 팝업 URL 폴링이 아니라 BroadcastChannel(redirect bridge)로 응답 수신 → 리디렉션 페이지가 broadcastResponseToMainFrame()을 실행해야 하는데 정적 빈 HTML이라 미송신 → 본창 timed_out(사용자 보고, 코드로 특정). 같은 주소(/msal-redirect.html)를 rewrite로 유지해 Entra 재등록 불필요. popupBridgeTimeout 60s→300s(첫 로그인 2FA·동의 시간 여유). 로컬 실측: rewrite 서빙 + 브리지 실행(empty_response=직접 방문 기대 동작) 확인 |
| 2026-07-13 | /datalab 상세 워크북 섹션을 본문 마지막(첨부 자료 뒤)으로 이동 | app/(public)/datalab/[slug]/page.tsx | 사용자 요청: 워크북(뷰어+다운로드+Excel 작업 패널)을 아래로 — 순서: 개요→레이아웃→함수→파이썬→분석 과정→산출 결과→이미지→링크→첨부→**워크북**→notes |
| 2026-07-14 | /datalab 상단 '분석 방법 사전' 워드클라우드(MethodCloud) — 빈도별 크기·호버 확대·클릭 팝업(파이썬 코드+설명, 전체/블록 복사) | lib/statMethods.ts, components/feature/datalab/MethodCloud.tsx, app/(public)/datalab/page.tsx, app/globals.css(.method-term) | 사용자 요청: 기초 통계(7)·회귀·통계모형(6)·머신러닝(11)·데이터 핸들링(10) 4구분 34종을 카테고리 클러스터(칩 팔레트 고정색 blue/violet/teal/amber, 배경 color-mix 55%)로 모아 배치. weight 1~5→13~25px(500/600), 클러스터 내 순서는 id 해시 결정적 셔플(SSR 안전). 팝업은 GlossaryPanel 모달 패턴(Escape·스크롤락·오버레이 클릭 닫기) — 요약·개념 설명·번호 코드 블록(블록별 복사=부분 복사)+전체 코드 복사(제목 주석 결합)+해석·주의 박스, 클립보드 실패 시 execCommand 폴백. 호버 scale 1.12는 사용자 요청 마이크로모션(reduced-motion 시 해제). 정적 데이터(DB 불요), 코드는 pandas·scipy·statsmodels·sklearn+보험 실무 예제. Playwright 실측: 호버 transform·전체/블록 복사 클립보드·Escape·콘솔 에러 0 |
| 2026-07-14 | MethodCloud를 사분면 2차원 그래프로 개편(md+) + 팝업 글자 확대/축소·파라미터 해설 | lib/statMethods.ts(difficulty·params 34종), components/feature/datalab/MethodCloud.tsx(QuadrantChart·ClusterCloud 분리) | 사용자 요청: ① 사각형을 사분면으로 나눠 카테고리별 색 구분·카테고리 이름은 사각형 바깥(위 2·아래 2), 가로축=빈도(중심 세로선에 가까울수록 자주)·세로축=난이도(중심 가로선에서 멀수록 어려움, 극단=어려움). 배치는 ResizeObserver 실측 후 layoutQuadrants — 기준좌표(빈도·난이도)+NUDGES 격자 후보(±150×±120, 가로 1.6 가중 비용 정렬)로 겹침 회피(실측 bounding box 검사 0건), 축 라벨(어려움↑↓·자주 사용·빈도 낮음)은 장애물 선점. md 미만은 기존 클러스터 클라우드 폴백. ② 팝업 헤더 가−/%(클릭=원복)/가+ 배율 0.8~1.6(함수형 setState로 연속 클릭 안전, 닫아도 유지), 본문·코드·파라미터에 적용. ③ 전 항목 params(주요 파라미터·옵션 4~6개 해설, 카테고리색 mono 이름+설명 dl) 추가 — 코드 섹션 뒤·해석 팁 앞. 호버 확대는 래퍼 div(translate)+버튼(scale) 분리로 인라인 transform 충돌 회피. 후속(같은 날): 사분면 카테고리 라벨 확대(라벨 13→16px·힌트 11.5→13.5px·점 2→2.5, 모바일 클러스터 12.5→14.5px) — 사용자 요청 |
| 2026-07-14 | Web-Excel 모바일 로그인 수정 — 모바일은 전체 리디렉션(loginRedirect) 흐름 | lib/msgraph.ts(isMobileBrowser·buildMsalConfig·acquireTokenSilentOnly·beginRedirectLogin·completeRedirectLogin), app/msal-redirect/page.tsx(톱레벨 복귀 처리 추가), components/feature/datalab/ExcelLaunchPanel.tsx(모바일 분기·복귀 재개), output/msgraph_setup.md | 사용자 보고: 모바일에서 로그인 에러 — 원인은 팝업 전용 loginPopup(모바일은 팝업 차단/새 탭 분리/브리지 유실). 수정: UA 기반 isMobileBrowser() 분기 — silent 실패 시 sessionStorage 보류 컨텍스트(datalab:webexcel:pending, TTL 10분) 저장 후 loginRedirect(redirectStartPage=현재 URL). 리디렉션 페이지는 팝업(broadcastResponseToMainFrame)·톱레벨(동일 buildMsalConfig PCA로 handleRedirectPromise→원래 페이지 자동 복귀) 겸용. 복귀 시 패널이 completeRedirectLogin으로 토큰 회수→업로드 자동 재개, 자동 새 창 대신 '지금 열기' 링크(제스처 부재로 차단). 기존 /msal-redirect.html URI 재사용 — Entra 추가 등록 불필요. Playwright 실측: 모바일 UA→authorize 리디렉션(interactionType=redirect)+보류 저장, 취소 복귀 정리(보류·interaction 플래그 제거), PC 팝업 회귀(interactionType=popup), 직접 방문 안내 |
| 2026-07-14 | /datalab 파이썬 실행기(PyRunner) — 브라우저 안 Pyodide 실행 + 드롭다운 코드 로드 + 폴더 저장/불러오기 | lib/pyRunner.ts, components/feature/datalab/PyRunner.tsx, MethodCloud.tsx(실행기로 보내기·통합), lib/statMethods.ts(methodFullCode) | 사용자 요청: 사전 코드를 바로 실행 — Pyodide(WASM, CDN v0.26.2)로 서버 없이 브라우저 실행(최초 1회 다운로드·이후 캐시, loadPackagesFromImports로 필요한 패키지만·가볍게). 드롭다운=샘플 데이터 생성기+34종(카테고리 optgroup), 팝업 헤더 '▶ 실행기로 보내기'(코드 보이는 상태 유지+복사 병행). 샘플 생성기는 예제 파일명과 일치하는 claims.xlsx·policy.xlsx(600행 합성)+df/claims/policy 변수 준비 → 예제 대부분 그대로 실행됨. stdout/stderr 스트리밍+마지막 표현식 repr, matplotlib은 AGG로 PNG 캡처 렌더(비대화형 경고 필터). 데이터 업로드(CSV·XLSX)는 가상 FS에 파일명 그대로. 폴더 저장/불러오기=File System Access API(Chrome·Edge 한정 노출) — analysis.py+workspace.json+데이터 파일(FS 생성분 포함)을 PC 폴더에 저장, 로드 시 코드·데이터 동시 복원. lifelines·xgboost·lightgbm 미지원 고지 |
| 2026-07-14 | /datalab 파이썬 실행기 셀(단락) 단위 실행으로 개편 + openpyxl 로딩 버그 수정 | components/feature/datalab/PyRunner.tsx(셀 UI 전면 재작성), lib/pyRunner.ts(ensureExcelSupport) | 사용자 요청: 주피터처럼 단락별 실행 — 변수가 셀 사이에 유지되어 앞 셀에서 데이터 속성 확인·변수 설정 후 다음 셀에서 사용. 셀 분할: `# %%` 구분자 우선, 없으면 사전 블록 제목(# ── ──) 기준(헤더 주석뿐인 첫 청크는 다음 셀에 병합), 통짜면 1셀. 셀마다 ▶ 실행(Shift+Enter)·자동 높이·+셀/삭제, 상단 ▶▶ 전체 실행(오류 셀서 중단)·출력 지우기·변수 초기화(del globals 사용자 변수만, 데이터 파일 유지). analysis.py는 # %% 구분자로 직렬화(그대로 재실행 가능)·복원. **버그 수정**: 이 Pyodide 빌드는 openpyxl을 loadPackage 번들로 제공하지 않아(import openpyxl·loadPackage 모두 ModuleNotFound) 샘플 to_excel이 조용히 실패→claims.xlsx 미생성이었음. ensureExcelSupport로 loadPackage 실패 시 micropip.install('openpyxl') 폴백(1회 메모, 실패는 사용자에 노출). Playwright 실측(파일 지속성·openpyxl 실패 격리 진단 후): 샘플 생성→claims/policy.xlsx 실존 확인, 기술통계량 2셀 완주(셀2가 셀1 df 사용), 사용자 시나리오(셀A 90분위 q90 설정→셀B q90 필터 60/600), 셀 그래프 PNG, 변수 초기화, 콘솔 에러 0 |
| 2026-07-14 | /datalab 파이썬 실행기 단계 기반 셀 분리 + 업로드 파일명 자동 인식 + 실행기 배경색 구분 | components/feature/datalab/PyRunner.tsx(splitIntoCells 단계화·buildLoadCell·addDataFiles generateLoadCell), lib/pyRunner.ts(pandas DeprecationWarning 필터) | 사용자 요청: ① 코드를 단계로 분리 — [1단계 로드·속성 확인(read_*+describe/columns 등 일반 확인)]과 [분석(특정 열 이름 사용, 사용자 판단 필요)]. classifyLine(setup/load/inspect/analysis, SPECIFIC_COL=대괄호 문자열·groupby계열 정규식, GENERIC_INSPECT=describe/head/columns 등)로 '특정 열 이름 첫 사용 줄'을 경계로 삼아 그 앞(로드·확인)을 첫 셀로, 경계 직전 설명 주석은 분석 셀로 이월. 로드 셀에 열 목록 미출력 시 detectDfVars로 감지한 모든 df에 `print("<v> 열:", <v>.columns.tolist())` 자동 추가, 분석 첫 셀엔 `# ▸ 분석 단계 — 실제 열 이름에 맞게 수정 후 실행` 배너. # %% 명시 구분자는 최우선. ② 데이터 업로드 시 실제 파일명·확장자 인식해 buildLoadCell(read_csv/read_excel+shape·columns·dtypes·head)을 맨 위 셀로 자동 생성(폴더 복원은 generateLoadCell=false로 미생성). ③ 실행기 루트를 color-mix(chip-blue-bg 45%) 연블루 패널+테두리로 감싸 흰 카드와 구분(제목에 🐍). Playwright 실측: desc-stats 3셀(로드+describe+열목록 / 분석배너+claim_amt / groupby), join-merge 2-df 열목록 각각, mydata.csv 업로드→자동 로드셀 생성·실행 시 실제 6열 표시, 샘플→desc-stats 전체 실행 3셀 완주(Pyarrow 경고 억제), 배경색·콘솔 에러 0 |
| 2026-07-14 | /datalab 실행기 후속 3건: 업로드 시 로드 셀 교체(중복 제거)·다크 코드 셀·+셀(아래에) | components/feature/datalab/PyRunner.tsx(cellHasLoad·addDataFiles 교체 로직·셀 스타일) | 사용자 보고: 상관분석 로드 후 업로드하면 기존 로드 셀 위에 새 셀이 추가돼 중복(policy.xlsx + mydata.csv 두 로드 셀). 수정: ① addDataFiles에서 cellHasLoad로 기존 로드 셀 인덱스를 찾아 그 자리를 교체(분석 셀 유지, 없을 때만 상단 추가), 알림도 '교체' 문구. ② 코드 입력부 textarea를 다크 에디터로 바꿔 실행 셀 구분 강화, 셀 좌측 3px primary 액센트, 패널 블루 45→60%. ③ 셀 우측 '+ 셀'→'+ 셀(아래에)'(현재 셀 바로 아래 추가 명확화). Playwright 실측: correlation 3셀→업로드 후에도 3셀 유지(policy.xlsx 잔존 0·셀1 mydata.csv), 교체 셀 실행 시 실제 열 표시, 콘솔 에러 0 |
| 2026-07-14 | /datalab 실행기 AI 어시스턴트: 셀별 오류 수정/제안 + 요청 기반 코드 생성 + 코드 셀 배경 옅게 | lib/pyAssist.ts(신규), app/api/datalab/py-assist/route.ts(신규 공개 POST), lib/pyRunner.ts(collectDataSchema·경고 필터), components/feature/datalab/PyRunner.tsx(AI UI) | 사용자 요청: ① 셀별 '✦ AI 오류 수정/제안' — collectDataSchema로 로드된 DataFrame의 열·자료형·크기(+파일 목록) JSON을 수집해 셀 코드·오류와 함께 Claude(sonnet-4-6)에 전달, 고친 코드를 제안 패널(설명+다크 코드+이 셀에 적용/아래 새 셀로/닫기)로 표시. ② 러너 상단 '✦ AI에게 코드 요청' 입력 — 요청+스키마+이전 셀 코드로 새 코드 생성해 설명 주석과 함께 새 셀 추가. 서버 전용 ANTHROPIC_API_KEY(요약과 동일 SDK), 입력 길이 상한, 스키마(값 아닌 열 이름/자료형만) 전송 고지. pyAssist는 ```python 블록 파싱(설명=블록 앞 텍스트). ③ 코드 셀 배경 #171a20→#2f3540(옅은 차콜). pandas 경고는 개행 시작이라 message 정규식 미포착 → DeprecationWarning/FutureWarning 카테고리로 억제. Playwright 실측(샘플 df 로드 후): AI 생성=policy.groupby('product')['premium'] 실제 변수·열로 막대그래프 코드 생성·실행 완료(그래프), AI 오류 수정=존재X 열 '보험료'→'premium' 진단·수정·적용·재실행 성공(mean 출력), 코드 bg rgb(47,53,64), 콘솔 에러 0 |
| 2026-07-14 | /datalab 분석 방법에 통계 진단·ML 평가지표 코드 추가 + 로지스틱 bool 버그 수정 | lib/statMethods.ts | 사용자 요청: 산출뿐 아니라 진단·평가도 포함. 전통 통계 — 회귀(VIF 다중공선성·Breusch-Pagan 등분산·Durbin-Watson 자기상관·잔차 Shapiro), 로지스틱(VIF·McFadden pseudo R²·정확도·ROC-AUC·PR-AUC·혼동행렬), GLM(과산포 Pearson chi2/df·Deviance/df·quasi-Poisson), t-검정(집단 Shapiro·Levene·Cohen's d), ANOVA(Levene·잔차 Shapiro·eta²·등분산 위배 시 Kruskal), 카이제곱(Cramér's V·표준화 잔차) 진단 섹션 추가. 머신러닝 — 의사결정나무·랜덤포레스트·그래디언트부스팅(HistGB)·SVM·KNN에 '성능 평가' 자체완결형 섹션(train_test_split→학습→정확도·ROC-AUC·PR-AUC·혼동행렬·classification_report; SVM/KNN은 StandardScaler 파이프라인). ML 예제가 X_tr 외부정의에 의존하므로 평가 블록은 df로 자체 분할해 바로 실행. **버그 수정**: 로지스틱 smf.logit이 bool형 lapsed를 endog로 못 받던 기존 오류 → 사본 d=df.assign(lapsed=astype(int))로 변환(원본 불변). Playwright 실측(샘플 로드 후 전체 실행): 회귀 진단(VIF/BP p=0.26/DW=1.81/Shapiro), 로지스틱(VIF/McFadden/정확도0.542/AUC), 의사결정나무 평가(정확도/AUC/혼동/리포트), ANOVA(Levene/eta²=0.004), 카이제곱(Cramér V=0.104), t검정(Shapiro/Levene/Cohen d) 모두 오류 0 |
| 2026-07-14 | /datalab 데스크톱 2탭(데이터 분석 / 데이터 분석 예제) + 에러분석 반영을 새 셀로(원본 주석 제거) | app/(public)/datalab/page.tsx, components/feature/datalab/DataLabTabs.tsx(신규), components/feature/datalab/PyRunner.tsx(applyErrModal·모달) | 사용자 요청 2건: ① 에러분석 반영 시 원본을 '# 에러내용'으로 주석해 다시 넣지 말고, 기존(오류) 셀은 위에 그대로 두고 수정된 코드만 담은 새 셀을 바로 아래에 추가(buildErrorFixCode 제거, applyErrModal이 splice로 아래 새 셀 삽입, 모달 미리보기도 fixedCode만). ② /datalab에 데스크톱(md+) 전용 2탭 — '데이터 분석'(MethodCloud 워드클라우드+파이썬 실행기), '데이터 분석 예제'(검색·정렬+DataPostCard). DataLabTabs는 두 섹션을 한 번만 렌더하고 반응형 클래스(md:block/md:hidden)로 표시 제어(실행기·Pyodide 상태 유지), 탭 바는 hidden md:flex. 모바일(<md)은 탭 없이 두 섹션 스택(현 상태 유지). Playwright 실측: 에러 반영→셀 [1,3]→[1,3,5] 원본 유지+아래 새 셀 깔끔한 수정본(원본 # 주석 없음), 데스크톱 탭 전환(analysis/examples display 토글)·모바일 탭바 none+둘 다 block, 콘솔 에러 0 |
| 2026-07-14 | /datalab 실행기 4건 수정: 에러 반영 후 상태 잔존·스피어만 scipy·콤보→클라우드 강조·출력 접기 | components/feature/datalab/PyRunner.tsx, lib/pyRunner.ts, components/feature/datalab/MethodCloud.tsx, app/globals.css | 사용자 보고 4건: ① 에러분석 반영 후에도 '오류 — 트레이스백 확인' 잔존 → applyErrModal이 setErrModal 업데이터 안에서 setCells를 호출(중첩 setState)해 상태 리셋이 유실되던 것을 errModal 의존 콜백으로 분리(+aiError null). ② 상관분석 스피어만은 pandas가 scipy 지연 import → runPythonCode에서 method="spearman|kendall" 정규식 감지 시 scipy loadPackage 선로딩(기본값 반영). ③ 콤보박스에서 방법 선택/실행기로 보내기 시 워드클라우드 해당 항목 강조 — RunnerLoadRequest.methodId 추가, PyRunner onLoadMethod 콜백→MethodCloud highlightId→.method-term-active(primary 아웃라인+펄스, reduced-motion 해제). ④ 셀 실행 결과 접기/펼치기 토글(outputCollapsed, 그래프 수 표기). Playwright 실측: 반영 후 상태 텍스트 공백(오류 사라짐), 스피어만 완료(-0.042 무오류), correlation 선택→'상관분석' 아웃라인 rgb(62,106,225)·샘플 선택 시 해제, 출력 접기 시 pre 숨김, 콘솔 에러 0 |
| 2026-07-14 | /datalab 변수 반영=변수 선택 드롭다운 + 에러분석=팝업(반영 시 '# 에러내용' 주석) | components/feature/datalab/PyRunner.tsx(openVarPicker·ErrorFixModal·buildErrorFixCode), lib/pyAssist.ts(vars 대상 변수) | 사용자 요청: ① 변수 반영 → collectDataSchema에서 실제 세션 DataFrame 변수 목록을 파싱해 드롭다운(+AI 자동 선택)으로 보여주고, 고른 변수로 이 셀의 변수를 대체(vars 모드 request=대상 변수, 프롬프트가 그 변수로 강제). ② 에러분석 → 인라인 제안 대신 팝업(ErrorFixModal: 에러 요약 errorSummaryOf·진단 설명·미리보기, Escape·오버레이 닫기), '반영' 버튼 클릭 시 기존 코드를 '# 에러내용 — <요약>'으로 주석 처리(실행 불가)하고 아래에 수정본 추가. buildErrorFixCode 마커 '# 에러 수정'→'# 에러내용'. Playwright 실측: 변수 반영 드롭다운=[policy,claims,df,AI 자동 선택], policy 선택→mydf→policy(로직 유지), 에러분석 팝업(KeyError '나이' 요약+age 진단+# 에러내용 미리보기), 반영→셀이 # 에러내용으로 시작+수정본 실행 성공(mean 46.03), 콘솔 에러 0 |
| 2026-07-14 | /datalab 셀별 AI 3버튼 재구성(변수 반영·에러분석·AI 제안 입력) + 워드클라우드 웹 미지원 표시 | components/feature/datalab/PyRunner.tsx(runCellAssist·buildErrorFixCode·헤더 3버튼), lib/pyAssist.ts+route(edit·vars 모드), lib/statMethods.ts(webSupport·webNote), components/feature/datalab/MethodCloud.tsx(webTermStyle·설명 블록) | 사용자 요청: ① 셀 헤더에 좌→우 '변수 반영'(vars 모드: 실제 세션 변수에 맞게 변수명만 조정, 로직·열 유지)·'에러분석'(fix 모드: 오류 셀에서만 활성, 원본을 '# 에러 수정'으로 주석 처리+수정본을 아래에)·'✦ AI 제안'(누르면 우측 입력창 활성→edit 모드로 셀 수정·추가 요청). 전체(러너) AI 요청은 유지. 3버튼 모두 제안 패널로 검토 후 적용. ② 워드클라우드: 웹 실행기(Pyodide) 미지원 방법을 별도 색으로 — 생존분석(lifelines·webSupport none)=회색, 그래디언트 부스팅(LightGBM·partial)=점선 밑줄, 하단에 회색/점선 범례+사유 설명 블록. Playwright 실측: 3버튼 순서·에러분석 오류시만 활성, 변수 반영(mydf→df, product/premium 유지), 에러분석(data→df, # 에러 수정 주석+수정본, 적용·실행 성공 mean 46.03), AI 제안(입력 활성→'내림차순+막대그래프' 요청 반영), 워드클라우드 생존분석 rgb(154,156,161)·GB dashed·설명 lifelines 포함, 콘솔 에러 0 |
| 2026-07-14 | /datalab 대규모 보완(분포·시뮬레이션·규제·교차검증 확장 + 데이터 소스 3종 + 데이터 핸들링 셀 콤보박스 세분화 + 셀 이동·순서·상태색·접기색·팝업 뒤로가기) & /apps 머신러닝 자동분석 브로셔 팝업 | lib/statMethods.ts, lib/wrangleSnippets.ts(신규), lib/useHistoryDismiss.ts(신규), components/feature/datalab/{PyRunner,MethodCloud}.tsx, components/feature/{BrochureButton(신규),PdfViewer}.tsx, app/(public)/apps/page.tsx, scripts/gen-datalab-samples.mjs(신규), public/datalab/samples/{policy,claims}.xlsx, public/apps/ml-auto-flow-brochure.pdf | 사용자 요청 8+1건: ① 기초통계 '분포 적합·난수 생성' 새 방법(정규·로그정규·감마·지수·포아송·음이항 pdf/pmf·MLE 적합+AIC/KS·난수 생성·집합손해 몬테카를로·부트스트랩) + 선형·로지스틱에 시뮬레이션 섹션(합성데이터 계수 복원·부트스트랩 CI·몬테카를로 예측). ② 샘플 데이터 3종 소스: 호스팅(policy·claims.xlsx, exceljs 스크립트 생성)·내 파일 업로드(기존)·URL, 모두 로드셀 자동 생성. ③ Ridge/Lasso 특정 alpha 적합(+ElasticNet) + 선형·로지스틱·GLM 규제/제약 섹션(penalty·C·fit_regularized·positive). ④ 교차검증·튜닝 확장(KFold/Stratified/Repeated/TimeSeriesSplit/GroupKFold·cross_validate 다중지표·cross_val_predict·nested). ⑤ 모든 팝업 뒤로가기 시 닫힘(useHistoryDismiss: pushState+popstate, StrictMode 안전, 직접 닫으면 유령 항목 제거) — 사전·에러·PdfViewer 전체보기·브로셔. ⑥·⑦ 데이터 핸들링을 wrangleSnippets로 세분화(Join inner/left/right/outer/cross·Concat 행/열·Split 열분리/explode/청크/조건·Groupby sum/mean/agg/transform/filter 등 11그룹)→상단 로드 드롭다운에서 wrangle 제거하고 각 셀 '데이터 핸들링 ▾' 콤보박스로 삽입, 사전 wrangle 팝업은 '실행기로 보내기' 제거+셀 콤보박스 안내. ⑧ 실행결과 접기/펼치기 바를 slate 틴트(--chip-slate)로 output과 구분. ⑨ 셀 ▲/▼ 이동 + 위치번호·실행순서 In[n]·상태 색(대기/실행/완료/오류). +/apps 머신러닝 자동분석 아래 ML Auto Flow 브로셔(버튼→팝업 인라인 열람, 328KB). typecheck·build 통과. Playwright 실측: 브로셔 팝업·뒤로가기 닫힘(URL 유지, 직접 닫힘도 마커 제거), 사전 팝업 뒤로가기, wrangle 팝업 버튼 제거, 코드 드롭다운 3카테고리, 샘플 policy.xlsx 로드(600×16 read_excel)·wrangle 스니펫 삽입, 교차검증(5셀)·로지스틱(규제 fit_regularized·시뮬레이션 계수복원 age0.04/ratio2.03)·분포(AIC lognorm·plot) 전체 실행 완료, 셀 이동 스왑·In[n] 증가·slate 접기바, 콘솔 에러 0 |
| 2026-07-16 | 확률분포 탭 QQ-plot + 적합 코드 팝업 2탭(모델 적합/시뮬레이션)·분포별 몬테카를로·초보자 설명 + 빈도 차트 개선 | lib/{distributions,fitPython}.ts, components/feature/datalab/{DistQqDialog(신규),QqDialog,DistCard,DistCodeDialog,FitLab,DistChart}.tsx | 사용자 요청 3건: ① 확률분포 탭 분포별 QQ-plot — 데이터 없는 탭이라 단일=**평균·표준편차 정합 정규 대비**(적률 미정의 시 중위수·IQR 폴백 표기), 비교=**A vs B 분위수**(0.5~99.5% 99지점). quantileOf(연속 이분법·이산 누적) 신설·medianOf 위임, normalQuantile, QqChart export+축라벨 prop, 닫힌형 6종 대조 전부 일치. ② 몬테카를로는 분포마다 다르게 — 결과표 행 코드 팝업을 [모델 적합\|시뮬레이션] 2탭으로(DistCodeDialog tabs), 시뮬레이션 탭=분포별 전용 코드 11종(개념 설명·분포 특성·역변환 대안·표본↔이론 검증·분위수/VaR·초과확률·자기부담금/한도·무사고/대량사고, 파레토 발산 주의), 모든 파이썬 코드에 초보자 단계 주석(MLE란?·frozen·AIC 벌점·TVaR). 11종 전부 scipy 실행 성공. ③ 연간 건수 P(N=k) 차트 명확화 — stem 2벌 겹침→**관측=반투명 막대, 적합=선+점**(DistChart dots 옵션), 범례 추가, k=0 막대 클램프로 x축 0부터 |
| 2026-07-16 | /datalab 보완 4건 — 비교 B 초기화·적합 통계량 설명 팝업·최적 Top3·그래프 드래그 확대 | components/feature/datalab/{DistChart,DistCard,DistributionLab,FitLab}.tsx, StatInfoDialog.tsx(신규) | 사용자 요청 4건: ① 비교 B 패널 'A와 동일하게' 버튼(A의 분포·파라미터 복사). ② 적합 결과 통계량 8종(logL·AIC·BIC·KS D/p·A²·χ²/p) 설명 팝업 — 의미·KaTeX 수식·판단기준(ΔAIC/ΔBIC 관례·KS 1.36/√n·χ² E<5)·주의(모수 추정 시 p 관대). **열 이름 클릭=정렬과 충돌 방지 위해 열 이름 옆 ⓘ 아이콘으로 열기**(설계 판단). ③ 결과표 위 '최적 적합 Top 3' 메달 칩(현재 정렬 기준·값 표시, 클릭=오버레이, 정렬 변경 시 갱신). ④ DistChart 드래그 구간 확대 — 선택 하이라이트→ChartZoomDialog 팝업(xDomain 재렌더, 마커 유지, bar 경계 클램프, 빈 구간 안내), DistChart 공용이라 적합·확률분포·빈도 PMF 전부 적용, 터치는 스크롤 보존 위해 제외, setPointerCapture try/catch(합성 이벤트 안전). Playwright 실측: B 초기화, 확대(−1.8~1.0/160~5.4k), AIC ⓘ KaTeX, Top3 정렬 전환 갱신, 콘솔 0 |
| 2026-07-16 | /datalab 4번째 탭 '모델 적합' — 스프레드 데이터 입력→분포 적합·검정·QQ·몬테카를로 | lib/{fitData,pyFit,fitPython}.ts(신규), components/feature/datalab/{FitLab,DataSheetDialog,QqDialog}.tsx(신규), DistChart(bar 변형·미세 밀도 지수 눈금), DataLabTabs(4탭), app/(public)/datalab/page.tsx | 사용자 요청: 스프레드 팝업 붙여넣기 입력, 3형식 **자동 감지+사용자 확인**(오버라이드 셀렉트) — ①1열 개별 값→연속형만 ②2열 연도+값→**빈도·심도 동시** ③3열 최소·최대·건수(그룹)→연속형만. 첫 행 이름(헤더) 자동 인식(감지 힌트+차트 축 라벨). 확정 시 좌 요약/우 empirical PDF·CDF(그룹은 구간 막대+ogive), 연도+값은 연도별 건수 표+PMF(빈 연도 0건 채움). 분포 선택 전체 선택/해제+개별(조건 미충족은 사유와 비활성). **적합 계산은 Pyodide scipy(사용자 결정)** — pyRunner 싱글턴 재사용, 입력은 가상 FS JSON, 결과 json.dumps: 개별=MLE(.fit floc=0)+logL·AIC·BIC·KS·A², 그룹=구간 우도 Nelder-Mead(log 모수)+χ², 빈도=포아송·음이항(r 프로파일)·이항(n 정수 탐색)+χ². 결과표 열 클릭 정렬(AIC 기본·logL만 내림차순)·행 클릭 오버레이(기본 AIC 1위)·행별 QQ 팝업(45°+150분위)·파이썬 재현 코드 팝업(실행 엔진과 같은 방법·데이터 임베드 상한 2000), 하단 몬테카를로(심도만=분위수/빈도+심도=집합손해 S=ΣX·VaR·TVaR, 선택 분포 반영). 샘플 데이터 버튼(결정적 PRG). 검증: 샘플 187건 로그정규 μ=7.198/σ=0.8875 복원(생성 7.2/0.85)·포아송 λ=18.7·음이항 포아송 수렴, **UI 안내 코드 로컬 scipy 실행 = 브라우저 결과 소수 4자리 일치**(logL −1589.1218/χ² 1.0639), 그룹 6구간 χ² 전환·조건 제외, 정렬·오버레이·QQ·모바일 4탭 248px 한 줄, 콘솔 0. 주의: dev 서버 중 build 실행 시 .next 충돌(RSC/404) — .next 삭제 후 dev 재시작으로 복구 |
| 2026-07-16 | /datalab 모바일 탭 분리 + 확률분포 평균·중위수 마커·분포 비교 모드 | components/feature/datalab/{DataLabTabs,DistChart,DistCard,DistributionLab}.tsx, lib/distributions.ts | 사용자 요청 3건: ① 모바일에서도 탭으로 분리 — 탭 바 항상 노출(모바일 짧은 라벨 분석/예제/확률분포, md+ 긴 라벨), 세 섹션은 한 번만 렌더하고 display만 토글(실행기·Pyodide 상태 유지). ② 그래프에 평균·중위수 기본 표시 — meanOf/medianOf 신설(연속=CDF 0.5분위 이분법, 이산=누적 0.5 첫 도달 k), PDF·CDF 양쪽 수직선(평균=앰버 파선·중위수=로즈 점선)+값 라벨, 체크박스 토글(기본 켜짐), 라벨 최대 3행 계단식 배치·평균≈중위수(x범위 1.5% 이내)면 한 줄 병합·평균 미정의(파레토 α≤1)나 범위 밖이면 생략. ③ 파라미터 상단 '비교' 토글 — A/B 두 패널(B는 **같은 종류** 분포 드롭다운, 밀도↔질량은 같은 y축 비교 불가로 제한), A 실선+B 파선 겹침, B는 A의 현재 분포·파라미터 복사본에서 출발, 통계량 A/B 두 열(같은 분포면 공통 수식 열 유지·다른 분포면 수식은 A·B 블록 각각), 파이썬 코드도 겹쳐 그리는 코드로 전환. 구현: 코드 조립부를 pySpec(p, 접미사)로 추출해 단일(접미사 없음)·비교(_a/_b)가 한 경로 재사용(변수 충돌 없음), 비교 시 각 분포는 **자기 도메인 안에서만 표본화**(감마 α<1·베타 pdf가 지지집합 밖에서 발산), DistChart 좌표 2자리 반올림으로 Node↔브라우저 Math.exp 1 ULP 차이발 하이드레이션 불일치 해소. 검증: 생성 파이썬 24종 전부 scipy 실행 성공(평균 inf도 np.isfinite 가드), JS meanOf/medianOf ↔ scipy 12종 전부 일치(감마 중위수 1.6783·파레토2 259.921·음이항 4·α=0.8 null↔inf), 콘솔 에러 0 |
| 2026-07-15 | /datalab 3번째 탭 '확률분포' 신설 — 연속형 8·이산형 3 분포 인터랙티브(파라미터↔그래프·수식·통계량) + 파이썬 코드 팝업 | lib/{distMath,distributions}.ts(신규), components/feature/datalab/{DistributionLab,DistCard,DistChart,DistCodeDialog,Tex,code-popup}.tsx(신규), components/feature/datalab/{DataLabTabs,MethodCloud}.tsx, app/(public)/datalab/page.tsx, package.json(katex), output/datalab_probability_distributions_plan.md | 사용자 요청: 데스크톱 3탭('데이터 분석'/'예제'/'확률분포'). 연속형(정규·로그정규·지수·와이블·감마·베타·파레토 2모수 Lomax·1모수 single-parameter) + 이산형(이항·포아송·음이항). 각 카드: 슬라이더+숫자 입력→PDF/CDF(이산 PMF 스템·CDF 계단) SVG 실시간, KaTeX 수식(PDF/CDF+통계량 기호식), 통계량(평균·분산·표준편차·왜도·초과첨도) 수식+값. **결정 3건**: (초기) 그룹+개별 카드 접기 → **후속 사용자 요청으로 개별 분포는 접기 대신 그룹 상단 '버튼 행' 셀렉터로 전환**(버튼 클릭=그 분포 하나만 표시, DistCard는 key로 remount해 파라미터 초기화, 그룹 접기는 유지), KaTeX 도입, 코드 팝업은 현재 파라미터값 반영+복사(실행기 전송은 범위 밖). 차트 라이브러리 없이 자체 SVG(고정 viewBox), 확률 계산은 자체 수치(lib/distMath: Lanczos logΓ·erf·정규화 불완전 감마/베타 연분수·이분법 분위수). 파레토 적률은 원적률→중심적률(α 조건부, 미정의는 '정의 안 됨(α>k)' 표기). 코드 팝업 프리미티브(copyText·CopyButton·CodeBlock)를 code-popup.tsx로 추출해 MethodCloud와 공유. typecheck·build 통과. Playwright 실측: 3탭·그룹/카드 접기, 감마 α 변경 시 곡선 reshape+통계 갱신, 전 11분포 통계 정확(로그정규 kurt 5.898·베타 kurt −0.857·파레토2 skew 4.648/kurt 70.8·음이항 skew 0.949·포아송 kurt 1/3), 코드 팝업 현재값 반영·뒤로가기 닫힘, 파레토 α=1 전 적률 미정의, 콘솔 에러 0 |
| 2026-07-17 | /datalab 보험 특화 1단계 — '보험·계리' 카테고리 8종 신설 + 사전 팝업 2탭(정의 및 방법/코드 적용, 이론 43종) + 코드 섹션 기본/고급 수준 + FitLab 면책·한도 절단·검열 적합 + 계리 샘플 3종 + 상단 메뉴 구분 | lib/{actuarialMethods,methodTheory}.ts(신규), lib/{statMethods,fitData,pyFit,fitPython}.ts, components/feature/datalab/{MethodCloud,FitLab,DataSheetDialog,QqDialog,PyRunner}.tsx, components/feature/SiteNav.tsx, app/globals.css, scripts/gen-datalab-samples.mjs, public/datalab/samples/{experience,triangle,mortality_table}.xlsx, tsconfig.json(_workspace 제외), docs/superpowers/specs/2026-07-17-datalab-actuarial-phase1-design.md | 사용자 요청(보험 데이터 분석 특화 + ML 보완 제안 → 1단계 승인, Workflow 32에이전트 오케스트레이션 후 하네스 검토): ① **보험·계리 8종**(위험률 산출=노출·조발생률·포아송 정확CI·A/E / 위험률 보정=Whittaker-Henderson 3차 차분행렬·스플라인 / **생존분석 웹 실행판**=KM·Nelson-Aalen·log-rank를 numpy 직접 구현해 기존 lifelines 항목의 webSupport:none 한계 우회 / 신뢰도=제한변동 1,082건 고전수치·Bühlmann·Straub / 지급준비금=chain-ladder·BF·Mack se / 순보험료=빈도×심도 vs Tweedie·상대도·오프밸런스·LEV / 보험료 기초=생명표→계산기수 Dx·Nx·Cx·Mx→정기/종신/연금·3이원 로딩 / 재보험=XL 레이어·QS·집합손해 MC VaR·TVaR). **statsmodels·lifelines·chainladder 금지**(Pyodide 제약)로 전부 numpy·scipy·sklearn 직접 구현, 인라인 합성데이터(seed 42) 자체완결. 전 섹션 로컬 파이썬 실행 검증 — Mack은 Mack(1993) RAA 공표값(52,135·se 26,909) 재현, Tweedie↔빈도×심도 상관 0.9999, LEV 닫힌형·quad·MC 삼중 대조. 별도 파일(statMethods 비대화 방지)+`import type`으로 순환 없음, 칩 색 rose. ② **사분면 wrangle→actuarial 교체**(wrangle은 이미 셀 콤보박스로 이동했으므로 하단 컴팩트 칩 스트립으로, 모바일 클러스터는 5카테고리 전부). ③ **팝업 2탭**(사용자 요청) — [정의 및 방법](METHOD_THEORY 43키: definition·formulas KaTeX 157식·usage·interpretation, 없으면 intro+tips 폴백=점진 확대 설계)·[코드 적용](기존 본문 그대로). ④ **기본/고급 수준**(사용자 요청: "K-평균이 K 최적화부터라 기초 분석엔 특정 K 지정이 먼저") — MethodCodeSection.level("basic"|"advanced", 미지정=기본) 도입, 43종 122섹션 전부 표시(기본 65·고급 57)·순서 기본→고급·레벨 칩+[전체|기본|고급] 필터. 기본 섹션 신설: kmeans(K=3 지정→군집 크기·중심·프로파일→PCA 2D 산점도), random-forest·svm·knn·gradient-boosting(지정 하이퍼파라미터 학습·평가), regularized(특정 alpha), pca(n_components=2), hierarchical(군집수 지정), distributions(지정 분포 적합) 등. wrangle 10종은 전 섹션 기본. 부수 발견·수정: nonparametric·normality 기존 섹션이 미정의 변수로 첫 줄부터 NameError/KeyError(기본 라벨과 모순되어 자체완결 2줄 추가). ⑤ **FitLab 면책 d·한도 u**(좌측 절단·우측 검열): 우도 비검열 log f(x)−log S(d)·검열 log S(u)−log S(d), 조건부 KS F*=(F−F(d))/(F(u)−F(d))·비검열만, A²·χ² '—', QQ 조건부 분위수. 검증: 로그정규 참 μ=7/σ=1 → μ̂ 7.0061(0.09%)·σ̂ 1.0011(0.11%) 복원(naive .fit은 σ 26.3% 편향), d=0·u=∞는 닫힌형 MLE와 1e-9 일치(회귀 0). ⑥ 샘플 3종(experience 800행·triangle 2016~2023×8·mortality_table 0~100세, 결정적 시드 분리로 기존 policy·claims 스트림 불변). ⑦ **SiteNav**(사용자 요청 "메뉴가 서로 구분 안 됨") — hover 필+현재 경로 활성 필(usePathname+useSearchParams는 Suspense 경계), 필 패딩 증가만큼 gap 축소로 상쇄해 lg 1024 9개 한 줄 유지(Playwright 실측). 4렌즈 적대 리뷰 11건(high 1: 면책 d>0일 때 집합손해 MC가 청구 기준 빈도×무조건부 심도로 과소 → 조건부 역변환 추출로 근본 수정) 중 8건 반영. typecheck·build 통과(365 페이지) |
| 2026-07-18 | /datalab ML 보완 2단계 — 손실함수 항목 + 그래프 셀 콤보박스 + PCA 2차원 결과투영 | lib/{statMethods,methodTheory}.ts, lib/plotSnippets.ts(신규), components/feature/datalab/PyRunner.tsx, docs/superpowers/specs/2026-07-17-datalab-actuarial-phase1-design.md | 사용자 요청(1단계 제안서의 2단계 승인, Workflow 8에이전트 후 하네스 검토): ① **손실함수·비용민감 학습**(model 카테고리 신규 항목): [기본]MSE·MAE·RMSE(이상치 1건이 MSE 키우는 시연) / [고급]Huber·분위수(pinball)→QuantileRegressor 90% 분위수=VaR 개념 / [고급]포아송·Tweedie deviance→순보험료 부스팅(HistGradientBoostingRegressor loss=poisson·d2_tweedie_score) / [고급]분류 log loss vs hinge·비대칭 비용 임계값 최적화(언더라이팅). 이론 키 추가(methodTheory 44키). ② **그래프 셀 콤보박스**(lib/plotSnippets.ts 신규 — 데이터 핸들링 콤보와 동형 PlotSnippet/Group·plotInsertCode): PyRunner 셀에 `그래프 ▾` 추가, 3그룹 — 탐색(히스토그램+KDE·box/violin·산점도+회귀선·상관 히트맵·산점도 행렬)·모델 진단(잔차·학습곡선·검증곡선·ROC·PR·캘리브레이션·리프트/게인)·해석(변수 중요도·순열 중요도·PDP·ICE). 진단·해석 스니펫은 자체완결(인라인 빠른 적합, '이미 model 있으면 준비 블록 지우세요' 주석). 전 스니펫 로컬 실행 검증. ③ **PCA 2차원 결과투영 확산**([advanced] 섹션 추가, kmeans·pca는 이미 2D 있어 제외): hierarchical(군집 라벨 PCA 투영)·decision-tree·svm·knn(PCA 2축 결정경계 DecisionBoundaryDisplay, '2D 투영 직관용' 명시)·pca(t-SNE 비선형 대안). 3렌즈 리뷰 5건 전부 low → 직접 수정: KNN 결정경계 학습정확도(거리가중이라 항상 1.0)→5겹 CV(0.830)로 교체, PDP/ICE 정수 특성 float 캐스팅(sklearn 1.9 대비), plotSnippets 준비블록 구분자 `# ── 준비 끝 ──`→`# --- 준비 끝 ---`(셀 분할 마커 충돌 회피), 손실 섹션3 제목 '포아송·감마·Tweedie'→'포아송·Tweedie'(코드 정합). MethodCloud 무변경(신규 항목·섹션은 기존 렌더·필터 자동 처리). typecheck·build 통과(365 페이지). 미착수 3단계: 불균형·캘리브레이션 항목·꼬리진단·GPD·제로팽창·확률분포 탭 VaR/TVaR |
| 2026-07-18 | /datalab 파이썬 실행기 matplotlib 한글 폰트(나눔고딕) 등록 — 차트 제목·라벨 한글 깨짐(□) 해결 | lib/pyRunner.ts, public/fonts/NanumGothic-Regular.ttf(신규, OFL 2MB) | 사용자 요청: Pyodide 기본 matplotlib은 DejaVu Sans만이라 한글 제목이 두부(□)로 렌더됨. `ensureKoreanFont(py)`(ensureExcelSupport와 동형 메모이즈) 추가 — public/fonts/에서 같은 출처 fetch→가상 FS 기록→font_manager.addfont→`rcParams["font.family"]=["NanumGothic","DejaVu Sans"]`+`axes.unicode_minus=False`(마이너스 U+2212는 DejaVu 폴백). 코드에 matplotlib/pyplot 있을 때만 1회 등록, 실패는 무시(폰트 없이도 실행 계속). .ttf라 listFsDataFiles(데이터 파일 목록) 미노출. Playwright 실측(3001, Pyodide 실행): font.family 적용·한글 결측 글리프 0·전체 결측 0·그림 렌더·콘솔 에러 0 |
| 2026-07-18 | /datalab ML·적합 3단계 — 불균형·캘리브레이션·이상치(ML 3항목) + 확률분포 VaR·TVaR·보험 용례 배지 + 적합 꼬리진단·GPD·제로팽창 | lib/{statMethods,methodTheory,distributions,fitData,pyFit,fitPython}.ts, components/feature/datalab/{DistCard,DistributionLab,FitLab}.tsx, components/feature/datalab/TailDiagnosticsDialog.tsx(신규) | 사용자 요청(2단계 제안서의 3단계 승인, Workflow 11에이전트 후 하네스 검토): ① **ML 3항목**(ml 카테고리): 불균형 데이터 처리(class_weight·임계값이동·비용곡선·PR-AUC, imbalanced-learn 금지 sklearn만) / 확률 캘리브레이션(calibration_curve·Brier·CalibratedClassifierCV isotonic/sigmoid, 요율 적용 관점) / 이상치 탐지(IsolationForest·LOF·EllipticEnvelope). methodTheory 47키. ② **확률분포 탭 VaR·TVaR**(DistCard): q 슬라이더(90~99.9%), VaR=ppf(q)·TVaR=E[X\|X>VaR] 치환적분(u=1−(1−q)e^{−y}), 비교모드 A/B, 코드 팝업 반영. **보험 용례 배지**(distributions.ts usage 필드, --chip 토큰): 심도/빈도/손해율 등. ③ **적합 꼬리진단**(TailDiagnosticsDialog): mean excess·Hill·log-log 생존을 JS로 계산(Pyodide 불요), 두꺼운 꼬리 판단. **GPD·ZIP/ZINB** 후보 추가(pyFit did 분기·빈도 커스텀 우도, 복원 검증). 3렌즈 리뷰 high 1건(TVaR 코드생성이 dist.expect로 두꺼운 꼬리 과소적분 — lomax α=1.5에서 39872 vs 참값 63633) → 치환적분으로 근본 수정(코드생성↔표시값 일치 복원, 63631). low 2건: 사분면 ml 11→14 겹침은 하네스 실측 확인, TVaR α≈1(파레토 극단꼬리) 수 % 과소는 u→1 배정밀도 saturation이 원인(Ymax 상향 무효 — 실증 확인)이라 근본해결(생존함수 isf)이 표시↔코드 일치를 깨므로 현 근사 유지+제약 주석. DB 미접촉. typecheck·build 통과(365 페이지) |
| 2026-07-18 | /datalab "엑셀 분석함수" 탭 신설(맨 앞) + "데이터 분석"→"파이썬 데이터 분석방법" 리네임 | lib/{excelFunctions,excelFunctionsData}.ts(신규), components/feature/datalab/{ExcelFunctionCloud(신규),DataLabTabs}.tsx, app/(public)/datalab/page.tsx, docs/superpowers/specs/2026-07-18-datalab-excel-functions-design.md | 사용자 요청: 파이썬 사전과 유사한 엑셀 함수 사전. 4사분면(기초통계·수학/검색·참조/데이터가공·동적배열/조건·논리·집계) **75함수** + LET·LAMBDA 별도 섹션(재귀 해찾기 포함), 함수명에 도입버전 **위첨자(¹⁹·²¹·³⁶⁵)**. 팝업=개념(초보자)→구문→인수(필수/선택)→예제 **[전체·기초·고급] 필터**(초보자→고급 진행)→주의→연관. 콘텐츠는 **Workflow 22에이전트**(카테고리 배치 저작 + 버전·구문 감사) 저작, '엑셀 함수 대백과' PDF 교차검증. 사분면 배치는 파이썬 MethodCloud 미러링(빈도 가로·난이도 세로, 겹침 회피 NUDGES), 실행기 없이 예제 수식 복사. Playwright 실측(사분면 겹침 0·버전 위첨자·예제 필터·팝업·탭 5개 전환·콘솔 0). 미착수(후속 요청): 데이터 핸들링 숨김 섹션(3면 작업흐름)·팝업 '엑셀 적용 코드' 탭(Python in Excel: xl()·print 미표시·Anaconda 패키지 제약) |
| 2026-07-18 | /datalab 파이썬 탭 후속 2건 — 데이터 핸들링 3면 접이식 패널 + 방법 팝업 'Python in Excel' 3번째 탭 | components/feature/datalab/MethodCloud.tsx, lib/{methodExcelCode,methodExcelCodeData}.ts(신규) | 사용자 요청 2건: ① 데이터 핸들링(wrangle 10종)을 사분면 바로 아래 **'보이기/숨기기' 접이식 패널**(기본 접힘)로 — 작업 흐름순 **3면(선택·필터/결합·집계/정제·변형**, amber/cyan/slate)으로 나눠 열람, 클릭 시 기존 팝업 재사용(WrangleStrip→WranglePanel). ② 방법 팝업에 3번째 탭 **'엑셀 적용 코드'**(Python in Excel=PY() 코드) 추가 — 공통 차이점 안내(**xl() 데이터 참조·print는 진단창·Anaconda 큐레이션 패키지만**) + 방법별 **패키지 상태 배지**(사용가능/일부제한/미지원)·차이점 노트 + 섹션별 적응 코드(**원본과 거의 동일/변경됨** 배지). 콘텐츠는 **Workflow 16에이전트**가 47방법 코드를 xl()·print·패키지 제약에 맞게 적응 저작(survival=lifelines·gradient-boosting=lightgbm은 statsmodels/sklearn 대체로 'partial'). METHOD_THEORY와 동형 레지스트리(id 키 추가로 확대). 방법 추출은 typescript transpileModule로 데이터 로드. Playwright 실측(3면 토글·3탭·available/partial 렌더·콘솔 0). typecheck·build 통과(/datalab 514kB) |
| 2026-07-18 | 확률분포·모델 적합 코드 팝업에도 '엑셀 적용 코드' 탭 추가(공용 팝업 자동) | components/feature/datalab/DistCodeDialog.tsx, lib/methodExcelCode.ts | 사용자 요청: 확률분포·모델 적합의 코드 팝업(공용 DistCodeDialog)에 '엑셀 적용 코드' 탭을 자동 덧붙임. 이들은 파라미터·데이터로 **코드가 동적 생성**되므로 사전 저작 대신 `toExcelPython`으로 경량 변환 — `plt.show()` 제거(셀이 마지막 그림 반환), 모델 적합의 `# 1) 데이터 입력` 뒤에 **xl() 대안 주석** 삽입. CodeTab에 note 필드 추가해 코드 위 공통 차이점(xl()·print 진단창·셀당 그림 1개·Anaconda 패키지) 안내(cyan 박스). 확률분포=2탭(파이썬 코드/엑셀 적용 코드), 모델 적합=3탭(모델 적합/시뮬레이션/엑셀 적용 코드). PIE_CODE_NOTE·toExcelPython은 methodExcelCode.ts에 공유. Playwright 실측(정규분포 plt.show 제거, 로그정규 적합 xl() 주석 삽입·노트 렌더, 콘솔 0). typecheck·build 통과(/datalab 517kB) |
| 2026-07-18 | 엑셀 분석함수 탭 — 신규 '데이터 참조·연산자' 섹션 + 유사 함수 8쌍 병합 + 팝업 탭 리네임 | lib/{excelFunctions,excelFunctionsData}.ts, components/feature/datalab/{ExcelFunctionCloud,MethodCloud,DistCodeDialog}.tsx | 사용자 요청 3건: ① 사분면 아래 **'데이터 참조·연산자' 섹션 신설**(dataref 카테고리) — **트림 참조 `.`**(A2:.A100 후행·A:.A 전체열 자동범위·TRIMRANGE, 2024/365)·**구조적 참조**(표 `[#All]`·`[@열]`)는 신규 저작(웹 검증), **스필 `#`·암시적 교차 `@`는 사분면→이 섹션으로 이동**. LambdaSection→ExtraSection 일반화(lambda·dataref 공용). ② **유사 함수 8쌍을 한 카드로 병합**(Workflow 8에이전트): REDUCE·SCAN / SUMIF·SUMIFS / COUNTIF·COUNTIFS / VSTACK·HSTACK / SORT·SORTBY / TAKE·DROP / VLOOKUP·HLOOKUP / QUARTILE.INC·EXC — 두 함수 관계·차이·예제 통합(AVERAGEIF·AVERAGEIFS 기존 병합과 일관). 조립 시 더블이스케이프·마크다운 `**` 정규화. ③ **팝업 탭 리네임**: '코드 적용'→'파이썬 코드 적용', '엑셀 적용 코드'→'엑셀 코드 적용'(MethodDialog·DistCodeDialog 공통). 함수 75→69개(병합 16→8·신규 2). Playwright 실측(병합 카드·dataref 섹션·트림참조 팝업 정확·리네임·콘솔 0). build 통과(/datalab 518kB) |
| 2026-07-18 | 파이썬 데이터 핸들링 접이식 패널 — 방법별 세부 스니펫 리스트 + 간단 코드 팝업 | components/feature/datalab/{MethodCloud,DistCodeDialog}.tsx | 사용자 요청: 데이터 핸들링 3면의 각 방법(예: 행·열 선택) **아래에 세부 스니펫**(열 선택·loc·iloc 등)을 칩으로 리스트. 방법명(굵게)=**기존 전체 팝업 유지**, 스니펫 칩=**간단 코드 팝업**(DistCodeDialog 재사용 → '파이썬 코드 적용'+'엑셀 코드 적용' 2탭, snippetInsertCode). METHOD_SNIPPET_IDS로 wrangle 방법 id→WRANGLE_SNIPPET_GROUPS 매핑(filter 그룹은 isin 분리, join+concat 결합). DistCodeDialog에 subtitle·hideFooter prop 추가(스니펫 팝업은 scipy 푸터 숨김). Playwright 실측(9방법 세부 스니펫 리스트·iloc 팝업 2탭·엑셀탭 노트·푸터 숨김·콘솔 0). build 통과 |
| 2026-07-18 | /datalab 탭 6개로 재배치 + 파이썬 실행기를 신규 '파이썬 코드 실행' 탭으로 분리 | components/feature/datalab/{DataLabTabs,MethodCloud,RunnerContext(신규),RunnerPanel(신규)}.tsx, app/(public)/datalab/page.tsx | 사용자 요청 2건: ① '데이터 분석 예제'를 **맨 오른쪽으로 이동 + '예제 데이터 분석'으로 리네임**. ② 신규 2번째 탭 **'파이썬 코드 실행'에 실행기(PyRunner) 배치** — MethodCloud(파이썬 데이터 분석방법)에서 실행기를 분리. 실행기 상태(loadRequest)는 **DataLabTabs가 소유하고 RunnerContext로 공유**, 방법 팝업 '실행기로 보내기' → sendToRunner가 코드 주입 + pyrun 탭 자동 전환. 탭 순서: 엑셀 분석함수·파이썬 코드 실행·파이썬 데이터 분석방법·확률분포·모델 적합·예제 데이터 분석(탭바 6개, 좁으면 가로 스크롤). 클라우드 강조(highlightId)는 실행기 분리로 비활성. 실행기 제한 안내·핸들링 삽입 안내 문구를 '파이썬 코드 실행' 탭 기준으로 수정. Playwright 실측(6탭 순서·실행기 이동·실행기로 보내기→탭전환+코드주입·콘솔 0). build 통과(/datalab 520kB) |

## 프로젝트 필수사항 (모든 에이전트 공통)

- **DB 공존**: 기존 보험 뉴스 Supabase 프로젝트(`hkrxnkntapcychtbxpmv`)에 `ib_` 프리픽스로 additive 추가. 기존 객체 DROP/ALTER 금지. 합산 운영은 `output/integration_bridge_view.draft.sql`(분리+브리지 뷰) 참조 — 현재는 미연결.
- **시크릿은 서버 전용**: `SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`는 Route Handler/Server Action에서만 사용. 클라이언트 노출 금지. `.env`는 커밋 금지.
- **테이블 프리픽스** `ib_`. 익명에게 테이블 UPDATE 권한 금지(조회수는 RPC `ib_increment_view`).
- **디자인 비협상 (v2, 2026-06-13 사용자 승인 완화)**: 단일 주 강조색 `--primary`(#3E6AE1)+`--brand-sky` 유지, **그라데이션 금지**, 크림 캔버스(`--page-bg`)·카드 화이트, 그림자는 카드 엘리베이션(`--shadow-card`/`-hover`)+스티키 한정 `--shadow-float`. 색상 트랜지션 0.33s + 카드 hover lift 예외. **추가 허용**: ① 헤딩 600/700 무게 대비(본문은 400/500, 300 금지), ② 글래스 패널(`.glass-panel`, 스티키 필터바·내비 한정), ③ 데이터 워터마크(`.bg-watermark-curve`, ≤5% 불투명도, 히어로·섹션 헤더 한정), ④ 칩 뮤트 팔레트 8색(`--chip-*`, FSA 테마칩·차트·강조 한정 스코프), ⑤ 카운트업·리빌 마이크로모션(reduced-motion 존중). 상세는 `tweakcn-tesla-theme` 스킬.
- **산출물 컨벤션**: 구조화 산출물(스키마·계약·리포트)은 `output/*.sql|json|md`, 중간물은 `_workspace/`. `output/*.sql`은 Supabase SQL Editor에서 실행하거나 자격증명 확보 시 `supabase-sync` 스킬로 적용.
- TypeScript는 `async/await`.
